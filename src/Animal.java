/**
 * This interface is provided (without any methods) just to
 * demonstrate that a subclass can know about its superclass. 
 * See the demo code in GetClassInfoDemo.java
 * 
 * BTW, an interface when used this way is called a "tagging"
 * interface. It's normal purpose is to tag a collection
 * of classes with a common label (the interface name).
 */
public interface Animal {

}
